# Local CRM secrets. Copy to `.env.local` and fill real values.
#
# Used by `pnpm send`, `/api/send`, and `pnpm postmark:check`.
POSTMARK_API_KEY=

# Required only for Clerk-backed dynamic groups and manual sync.
CLERK_SECRET_KEY=
